#!/usr/bin/env pwsh
param()

Set-StrictMode -Version Latest
$ErrorActionPreference = 'Stop'

# Replace with commands to remove the resources created by the sample.

Write-Host 'No cleanup actions defined. Update scripts/cleanup.ps1 when adding resources.' -ForegroundColor Yellow
