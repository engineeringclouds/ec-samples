#!/usr/bin/env pwsh
param()

Set-StrictMode -Version Latest
$ErrorActionPreference = 'Stop'

# Placeholder entry point for Windows/PowerShell users.
# Replace these commands with the actual workflow required to demonstrate the blog scenario.

Write-Host 'Running sample...' -ForegroundColor Cyan
# az deployment group create --resource-group $env:RESOURCE_GROUP --template-file src/main.bicep

Write-Host 'Sample complete. Update scripts/run.ps1 with real commands.' -ForegroundColor Green
